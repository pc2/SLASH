#ifndef PLATFORM_HPP
#define PLATFORM_HPP

namespace vrt {
    enum class Platform {
        HARDWARE,
        EMULATION,
        SIMULATION,
        UNKNOWN
    };

} // namespace vrt

#endif // PLATFORM_HPP